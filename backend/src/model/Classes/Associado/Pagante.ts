/*
import { Associado } from "./Associado";
import { sexo } from "../../../controller/types/sexo";
import { Endereco } from "../Endereco";
import { Telefone } from "../Telefone";
import { condicao } from "../../../controller/types/condicao";
export class Pagante extends Associado{
    constructor(nome: string,  dataNascimento: string, sexo: sexo, endereco: Endereco, telefone: Telefone, email: string, cpf: string, familia: string, localOrigem: string, condicao: condicao, uuid?: string, dataAssociacao?: Date)
    {
        super(nome, dataNascimento, sexo, endereco, telefone, email, cpf, familia, localOrigem, condicao, uuid, dataAssociacao);
    }
}
*/